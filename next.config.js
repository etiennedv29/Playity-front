/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["img.freepik.com", "static.vecteezy.com", "scienceline.org", "api.dicebear.com"],
  },
};

module.exports = nextConfig;
